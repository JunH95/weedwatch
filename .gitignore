# colcon
build/
install/
log/

# smoke/test artifacts
artifacts/
*.png
*.mp4

# python
__pycache__/
*.pyc
.venv/
venv/

# datasets & models are build artifacts — regenerate from (config, seed, SHA)
datasets/
models/
*.pt
*.blend
*.blend1

# AI Hub data is redistribution-prohibited. Never commit it.
data/aihub/

.vscode/

# 남의 저장소는 커밋하지 않는다. make cropcraft 가 고정된 SHA 로 가져온다.
third_party/
